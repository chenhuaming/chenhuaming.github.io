---
layout: post
title: LinkedList的简单实现
tag: js,快速排序
---

```javascript
function sort(array){
	quicksort(array, 0, array.length-1);
}
function quicksort(array, low ,high){
	if(high-low<2) return;
	var okpositon = good(array, low, high);
	quicksort(array, low, okpositon-1);
	quicksort(array, okpositon+1, high);
}
function good(array, low, high){
	var tmp = array[low];
	while(low<high){
		while(low<high&&array[high]>=tmp){
			high--;
		}
		if(low<high) array[low++] = array[high];
		while(low<high && array[low]<=tmp){
			low++;
		}
		if(low<high) array[high--] = array[low];
	}
	array[low] = tmp;
	return low;
}
(function test(){
	var array=[1, 3, 5, 4, 7, 6 , 3 , 2, 8];
	sort(array);
	console.log(array);	
})();

```
